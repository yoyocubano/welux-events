[build]
  base = "/"
  command = "npm config delete //registry.npmjs.org/:_authToken || true && npm install --legacy-peer-deps && npm run build"
  publish = "dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
